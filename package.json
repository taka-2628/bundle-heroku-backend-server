{
  "name": "bundle-heroku-backend-server",
  "version": "1.0.0",
  "description": "json backend server for BUNDLE",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Takashi Honzawa",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
